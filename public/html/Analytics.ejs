


<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Analytics</title>
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/my-urls">My URLs</a></li>
        <li><a href="/about">About</a></li>
      </ul>
    </nav>

    <h1>Analytics</h1>

    <% if (!isLoggedIn) { %>
    <p>Please log in to view your URLs and analytics data</p>
    <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Short URL</th>
          <th>Long URL</th>
          <th>User Agent</th>
          <th>IP Address</th>
          <th>Location</th>
          <th>Timestamp</th>
        </tr>
      </thead>
      <tbody>
        <% analytics.forEach((analytic) => { %>
        <tr>
          <td><%= analytic.shortURL.shortURL %></td>
          <td><%= analytic.shortURL.longURL %></td>
          <td><%= analytic.userAgent %></td>
          <td><%= analytic.ipAddress %></td>
          <td>
            <%= analytic.location.city %>, <%= analytic.location.country %>
          </td>
          <td><%= new Date(analytic.timestamp).toLocaleString() %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <% } %>
  </body>
</html> -->


<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Analytics</title>
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/my-urls">My URLs</a></li>
        <li><a href="/about">About</a></li>
      </ul>
    </nav>

    <h1>Analytics</h1>

    <% if (!isLoggedIn) { %>
    <p>Please log in to view your URLs and analytics data</p>
    <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Short URL</th>
          <th>Long URL</th>
          <th>User Agent</th>
          <th>IP Address</th>
          <th>Location</th>
          <th>Timestamp</th>
          <th>Visits</th> <!-- Added visits column header -->
        </tr>
      </thead>
      <tbody>
        <% analytics.forEach((item) => { %>
        <tr>
          <td><%= item.shortURL.shortURL %></td>
          <td><%= item.shortURL.longURL %></td>
          <td><%= item.userAgent %></td>
          <td><%= item.ipAddress %></td>
          <td><%= item.location.city %>, <%= item.location.country %></td>
          <td><%= new Date(item.timestamp).toLocaleString() %></td>
          <td><%= item.shortURL.visits %></td> <!-- Added visits column -->
        </tr>
        <% }); %>
      </tbody>
    </table>
    <% } %>
  </body>
</html>

